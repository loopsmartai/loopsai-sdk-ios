.DS_Store
/.build
/Packages
xcuserdata/
DerivedData/
.swiftpm/
.netrc
*.xcodeproj
Package.resolved

# Internal-only notes that must never reach the public consumer surface.
# Kept out of git entirely, but Claude Code still reads it locally.
/CLAUDE.md
